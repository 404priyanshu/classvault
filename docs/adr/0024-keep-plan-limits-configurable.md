# Keep Plan Limits Configurable

For the MVP, ClassVault keeps plan-based limits such as upload count, storage, roadmap generation quota, study room duration, and room capacity configurable through admin settings or environment configuration rather than hard-coding exact values in application logic. This supports post-launch tuning based on usage, cost, and abuse patterns without changing the domain model.
